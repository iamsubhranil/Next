arr = []
start = clock()
for(i = 0; i < 1000000; i++) {
    arr[i] = i + 1
}

sum = 0
for(i in arr) {
    sum = sum + i
}

end = (clock() - start)/clocks_per_sec

print(sum, "\n")
print("elapsed: ", end)
